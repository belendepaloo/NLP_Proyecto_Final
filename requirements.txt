# requirements.txt — primer archivo de dependencias del repo (antes no existia
# ninguno). Pineado contra un install real en Python 3.12 (ver Fase 0 del plan de
# ingenieria). Organizado por capa para que sea claro que pertenece a que parte.

# ---------------------------------------------------------------------------
# Core / compartido (investigacion + ingenieria)
# ---------------------------------------------------------------------------
torch==2.7.0
transformers==5.12.1
huggingface_hub==1.20.1
datasets==5.0.0
accelerate==1.14.0
pandas==2.2.3
numpy==2.2.6
scikit-learn==1.7.0
scipy==1.13.1
tqdm==4.68.3
requests==2.32.3
PyYAML==6.0.3
matplotlib==3.9.0

# ---------------------------------------------------------------------------
# processRawText (scraping + limpieza + chunking)
# ---------------------------------------------------------------------------
trafilatura==2.1.0
pysbd==0.3.4
tiktoken==0.13.0

# ---------------------------------------------------------------------------
# SAGE (paraphraser)
# ---------------------------------------------------------------------------
google-genai==2.10.0
# transformer_lens y sae_lens son pesados (descarga multi-GB de Gemma-2B + SAE) y no
# se instalaron en este venv de verificacion -- necesarios solo para SAGE/sps.py
# (Semantic Persistence Score). Confirmar version + tiempo de carga en CPU/GPU antes
# de la Fase 1 (ver riesgos abiertos en el plan).
transformer_lens>=2.0
sae_lens>=4.0

# ---------------------------------------------------------------------------
# SiMIA / DE-COP / DUALTEST (metodos de scoring MIA)
# ---------------------------------------------------------------------------
sentence-transformers==5.6.0
groq==1.5.0
openai==2.44.0
anthropic==0.112.0

# ---------------------------------------------------------------------------
# mia_common (cliente target unificado + settings)
# ---------------------------------------------------------------------------
pydantic==2.13.4
pydantic-settings==2.14.2

# ---------------------------------------------------------------------------
# agents/ (orquestador deepagents, Fase 2+)
# ---------------------------------------------------------------------------
deepagents==0.6.11
langgraph==1.2.6
langchain==1.3.11
langchain-google-genai==4.2.5
langchain-anthropic==1.4.7
tavily-python==0.7.26

# ---------------------------------------------------------------------------
# webapp/ (FastAPI + Jinja2, Fase 4)
# ---------------------------------------------------------------------------
fastapi==0.138.0
uvicorn[standard]==0.49.0
Jinja2==3.1.6
